group = 'com.giftandgo.assessment'
version = '0.0.1'

tasks.register('clean') {superClean ->
    superClean.setGroup('build')
    gradle.includedBuilds.each { includedBuild ->
        superClean.dependsOn(includedBuild.task(':clean'))
    }
}
